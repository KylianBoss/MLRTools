const path = require("path");

module.exports = {
  config: path.resolve("src-electron", "config", "database.cjs"),
  "migrations-path": path.resolve("storage", "migrations"),
  "seeders-path": path.resolve("storage", "seeders"),
};
